services:
  node:
    build:
      context: .
    ports:
      - 8080
    volumes:
      - data:./storage/data.sqlite
    environment:
      - TG_TOKEN=$TG_TOKEN
      - CHAT_ID=$CHAT_ID
endpoints:
  - path: /
    service: node
    port: 8080
